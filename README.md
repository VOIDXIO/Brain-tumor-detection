# Brain-tumor-detection
Built with Python, TensorFlow, and Keras, this Convolutional Neural Network (CNN) classifies MRI scans to detect brain tumors. It utilizes real-time image augmentation and Dropout layers to prevent overfitting and ensure high accuracy. The model processes raw MRI images to output a definitive "yes" or "no" binary prediction.
